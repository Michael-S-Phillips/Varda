# This was something I read about to get python to recognize modules.
# Turns out it might not be necessary but just gonna leave here for now anyway.
from setuptools import setup, find_packages

setup(name='SpecLab', version='0.1', packages=find_packages())
